### Set up Rspec

```bash
$ mkdir spec
$ touch spec/arena_spec.rb
$ touch spec/gladiator_spec.rb
$ rspec --init
   exist   .rspec
  create   spec/spec_helper.rb
$ rspec
No examples found.

Finished in 0.00036 seconds (files took 0.066 seconds to load)
0 examples, 0 failures
```
